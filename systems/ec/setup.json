{
  "reactions": [
    "jadeite = albite + quartz", 
    "clinopyroxene.jadeite = feldspar.albite. + quartz"
  ]
}
// parse:

/*

interface Species {
  phase: string
  endMember: string
}

let reactants = [] as Species[];
let products = [] as Species[];

reactions.foreach(rx => {
  let [reactantsString, productsString] = rx.split("=");
  
  let reactantsList = reactantsString.split("+").map(trim);
  let reactantsList.forEach(r => {
    let [ph, em] = r.split(".");
    reactants.push({phase:ph, endMember:em ?? ph});
  })

  let productsList = products.split("+").map(trim);
  let productsList.forEach(p => {
    let [ph, em] = p.split(".");
    products.push({phase:ph, endMember:em ?? ph});
  })
})
*/